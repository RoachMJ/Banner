{% extends 'layout.jinja2' %}
{% block content %}

<div>
    <div class="form-group">
            <label></label>
{## Note form action = {{ request.route_url is sending the input form.professor_name to the view config  #}
        <form action="{{request.route_url('create_professor')}}" method="post" class="form">
            <div class="form-group">
                <label for="body">{{form.professor_name.label}}</label>
                {{form.professor_name}}
                <button type="submit" class="btn btn-default">Submit</button>
            </div>
        </form>
    </div>


</div>

{% endblock content %}
